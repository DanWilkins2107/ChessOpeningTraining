// Excluded from Stryker in stryker.config.json: no test can observe a change to
// these values.

export const CSP_PLUGIN_NAME = 'csp';

export const ROOT_ROUTE_PATH = '/';

// An empty dependency list mutated to ["Stryker was here"] behaves the same.
export const SUBSCRIBE_ONCE = [];
