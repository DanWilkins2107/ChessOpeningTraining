# ChessOpeningTraining

Gates — vitest checks on the repo itself rather than the app — live in `meta/`;
their shape is documented in [meta/README.md](meta/README.md). Update this link
if that file moves.
