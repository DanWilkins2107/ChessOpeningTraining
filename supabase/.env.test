# A second local stack just for tests, so they never touch the dev stack's data.
# The db:test npm script loads this over config.toml: its own project, and
# ports moved 545xx -> 546xx.
SUPABASE_PROJECT_ID=ChessOpeningTraining-test
SUPABASE_API_PORT=54621
SUPABASE_DB_PORT=54622
